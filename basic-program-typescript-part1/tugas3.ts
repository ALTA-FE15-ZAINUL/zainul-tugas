function greet(name: string): string {
  return `Hello ${name}. Saya Javascript, bahasa yang sangat menyenangkan.`;
}

// outputnya
const inputName = "Zainul";
const greeting = greet(inputName);
console.log(greeting);
